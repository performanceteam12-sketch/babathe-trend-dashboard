# 바바더닷컴 패션 트렌드 대시보드 — Tasks

- [x] 프로젝트 문서 생성 (plan/context/tasks)
- [x] 프로젝트 CLAUDE.md 작성
- [x] 네이버 데이터랩 스크래퍼 작성 (`scraper/naver_datalab_scraper.py`)
- [x] Streamlit 대시보드 작성 (`dashboard/app.py`)
- [x] requirements.txt 작성
- [x] 로컬 실행 및 검증 (스크린샷 증명)

## 2차 리디자인 (2026-07-24)
- [x] 사이드바 3개 인덱스 구조 + 뉴트럴/블루 액센트 디자인 시스템 적용
- [x] 광고 소재 레퍼런스 큐레이션 갤러리 (업로드/태그/필터)
- [x] 경쟁사 모니터링 (4개 브랜드 × 4슬롯 업로드)
- [x] 업로드 기능 실사용 테스트, 빈 값 NaN 버그 수정

## 3차: 경쟁사 모니터링 자동 캡처 (2026-07-24)
- [x] search.naver.com 브랜드검색 PC/MO 영역 자동 캡처 (`scraper/competitor_monitor_scraper.py`)
- [x] Meta 광고 라이브러리 계정명 매핑(`ACCOUNT_MAP`) 확인 후 소재 2종 자동 캡처
- [x] 오늘자(2026-07-24) 4개 브랜드 × 4슬롯 = 16건 실제 캡처 및 대시보드 반영 확인
- [x] Windows 작업 스케줄러 `BabaderCompetitorMonitor` 등록 — 매주 목요일 17:00 자동 실행

## 4차: 인기 검색어 카드 레이아웃 + 패션잡화 추가 (2026-07-24)
- [x] 네이버 데이터랩 스크래퍼에 패션잡화(대분류, 서브카테고리 없음) 추가, TOP10 → TOP40으로 확장 (페이지네이션 처리)
- [x] `data/naver_top10/{slug}/{date}.csv` 구조로 변경 (카테고리별 폴더 분리)
- [x] 대시보드를 참고 이미지 스타일 2열 카드로 재구성 (배지, TOP3 강조색, 원본 링크, 스크롤 리스트)
- [x] 필터는 기존 그대로 유지(성별·연령 필터 추가 안 함), "전일자" 문구는 반영 안 함(최근 1개월 유지) — 사용자 확인됨

## 5차: 앱 실시간 검색어 카드 레이아웃 + 브랜드 통일 (2026-07-24)
- [x] "앱 실시간 검색어"를 탭+표 방식에서 4개 카드(브랜드별 배지·TOP3 강조·스크롤 리스트) 방식으로 재구성 (`render_rank_card` 재사용)
- [x] 채널을 더한섬닷컴/SSG(신세계몰)/W컨셉 3개 → 더한섬닷컴/신세계V/W컨셉/바바더닷컴 4개로 변경 (경쟁사 모니터링과 브랜드 통일)
- [x] 입력 폼은 "검색어 입력 / 수정" expander로 이동, 카드 위에서는 최신 입력만 보여줌
- [x] 실제 입력 테스트로 카드 반영 확인 후 테스트 데이터 정리

## 6차: 앱 실시간 검색어 자동 캡처로 전환 (2026-07-24)
- [x] 기존 "자동 수집 불가" 판단 정정 — 사용자가 실제 검색창 클릭 시 노출되는 인기/급상승 검색어 화면을 캡처해서 보여줘 재조사
- [x] 4개 사이트 실제 도메인(thehandsome.com/shinsegaev.com/display.wconcept.co.kr/babathe.com) robots.txt 재확인 — 신세계V·바바더닷컴은 문제 없음, 더한섬닷컴·W컨셉은 차단이나 사용자 승인 하에 저빈도 진행
- [x] `scraper/app_search_scraper.py` 작성 (사이트별 검색창 클릭 → 리스트 파싱). 더한섬닷컴은 "인기 검색어"와 "급상승 검색어"가 같은 오버레이에 섞여 있어 처음엔 잘못된 리스트를 잡았고, 빈 슬롯 없는 쪽을 골라내는 로직으로 수정해 "급상승 검색어"만 정확히 캡처하도록 고침
- [x] 4개 브랜드 × 10개 = 40건 실제 캡처 및 대시보드 카드 반영 확인, "새로고침" 버튼으로 대시보드에 연결
- [x] 카드에 자동 캡처/수동 입력 여부(`input_by`) 표시

## 7차: 광고 소재 레퍼런스 섹션 삭제 (2026-07-24)
- [x] 사용자가 "안 쓸 것 같다"고 해서 사이드바 인덱스 3개 → 2개(이번 주 인기 검색어 / 경쟁사 모니터링)로 축소
- [x] `page_ad_reference`/`load_ad_reference`/`save_ad_reference_row` 함수, 관련 CSS(`.ref-card`,`.tag-pill`), `AD_REF_*` 상수 전부 제거
- [x] `data/ad_reference/` 폴더 삭제 (비어 있어 손실 없음)
- [x] 재실행 후 남은 2개 페이지 정상 동작 확인

## 8차: 경쟁사 모니터링 카드 개선 + cp949 인코딩 버그 수정 (2026-07-28)
- [x] "새로고침" 클릭 시 실제로는 수집이 됐는데도 "수집 실패"로 뜨는 버그 근본 원인 발견: Windows 콘솔 기본 코드페이지(cp949)가 스크래퍼 출력의 특수문자를 인코딩 못 해 UnicodeEncodeError로 죽고, 그게 비정상 종료 코드로 이어짐
  - 3개 스크래퍼 모두 `main()`에서 stdout/stderr를 UTF-8로 강제 재설정, 대시보드의 subprocess 호출도 `PYTHONIOENCODING=utf-8` 환경변수 + UTF-8 디코딩으로 통일 (`run_scraper_script()` 공용 헬퍼로 정리)
- [x] 경쟁사 모니터링 이미지가 너무 작아서 안 보인다는 피드백 → 4열(PC/MO/메타1/메타2) 한 줄 배치를 "브랜드검색(PC+MO, 2열, 크게)" → "메타 광고 소재(2열, 크게)" 순서의 2단 구성으로 재배치, 인기검색어와 동일한 rank-card 템플릿 재사용
- [x] 브랜드검색 MO 캡처가 일부 브랜드만 네이버 검색창/탭이 그대로 남아 지저분하다는 피드백 → 광고 박스 자체의 상단·하단을 모두 크롭하도록 수정해 전 브랜드 통일
- [x] Meta 소재 출처: "광고관리자"는 로그인 필요 + 계정 소유자 광고만 조회 가능해 경쟁사엔 원천적으로 못 씀을 설명 → 사용자가 기존 공개 광고 라이브러리 유지를 선택, 정렬만 "최신순"(`sort_data[mode]=relevancy_monthly_grouped`)으로 변경
  - 최신순 정렬에서는 브랜드 자체 계정 광고가 초기 로드 목록 밖에 있을 수 있어 스크롤-재탐색 로직 추가 (W컨셉에서 실제로 필요했음)
- [x] 경쟁사 모니터링 CSV dedup 로직을 pandas merge에서 순수 파이썬 튜플 비교로 교체 (더 예측 가능하고 디버그하기 쉬움)
- [x] 4개 브랜드 × 4슬롯 = 16건 전체 재캡처로 최종 확인

## 9차: 브랜드검색 MO 5개 노출 + 메타 소재 정확도 개선 (2026-07-29)
- [x] 브랜드검색 MO 하단 썸네일이 3~4개만 보이고 잘리는 문제 → 모바일 페이지가 반응형이라 캡처 뷰포트를 390 → 700으로 넓히면 캐러셀 없이 5개가 다 펼쳐짐을 발견, `MOBILE_CAPTURE_WIDTH` 상수로 적용
- [x] 메타 소재를 "브랜드명 검색 결과" 대신 "검색창 자동완성(광고주 정확 일치)에서 얻은 view_all_page_id로 그 계정 전용 목록에 바로 진입"하는 방식으로 전환 (`PAGE_ID_MAP`) — 무관한 타사 광고가 섞이는 문제 해결
- [x] 정렬은 계속 "최신순" 유지
- [x] 소재 선택 로직에 `FASHION_KEYWORDS` 텍스트 매칭 추가 — 패션(의류) 관련 소재 우선 선택, 없으면 최신순 상단부터 채움
- [x] 카드 스크린샷 대신 실제 광고 이미지를 CDN URL에서 다운로드하는 방식으로 전환 (아바타 이미지는 alt 텍스트로 구분해 제외), 파일 확장자도 실제 포맷(.jpg)에 맞게 저장
- [x] 4개 브랜드 전체 재캡처로 최종 확인 (브랜드검색 MO 5개 노출 + 메타소재 2개 모두 패션 소재로 매칭됨)

## 10차: 메타 소재 형식 필터링 + 이미지 크기 통일 + 새로고침 버튼 (2026-07-29)
- [x] 메타 소재에서 카탈로그(다이나믹 프로덕트) 광고 제외, 컬렉션/이미지 광고만 선택 — 라이브러리 UI에 형식 필터가 없어 카드 텍스트의 개별 가격표기(00,000원) 반복 여부로 판별하는 `is_catalog_ad()` 추가
- [x] 브랜드마다 메타소재 이미지 크기가 다르게 보이는 문제 → 대시보드에서 고정 비율(4:5) 박스 안에 object-fit:contain으로 담아 카드 레이아웃 크기를 브랜드 간 통일 (원본 이미지는 그대로, 표시 방식만 변경)
- [x] "경쟁사 모니터링" 페이지 최상단에 새로고침 버튼 추가 — 브랜드검색 PC/MO + 메타소재까지 4개 브랜드 전체를 대시보드에서 바로 재수집 가능 (`run_competitor_scraper()`)
- [x] 4개 브랜드 전체 재캡처로 최종 확인

## 11차: 메타 소재 콘텐츠 사진 우선 + 중복 제거 (2026-07-29)
- [x] 제품 단독 컷(흰/단색 배경) 제외, 모델 착용·라이프스타일 사진만 사용 — 이미지 모서리 색상 표준편차로 판단하는 `looks_like_content_photo()` 추가 (PIL 기반)
- [x] 같은 소재가 여러 광고 항목에 중복 게재된 경우 메타소재 1/2에 중복으로 넣지 않도록 CDN URL의 미디어 ID로 dedup (`media_id_of()`, `seen_media_ids`)
- [x] 조건에 맞는 소재가 1개뿐이면 억지로 채우지 않고 메타소재 2를 명시적으로 비움 (`clear_slot()`)
- [x] 우선순위를 패션 → 뷰티/라이프/신발 → 그 외 3단계로 확장 (`BEAUTY_LIFE_SHOES_KEYWORDS` 추가, 기존엔 패션 아니면 바로 최상단 채우기였음)
- [x] 각 소재의 라이브러리 ID·게재 시작일을 로그에 남겨 출처 확인 가능하게 함
- [x] 4개 브랜드 재캡처 후 실제 이미지 확인 — 제품 단독컷 없이 콘텐츠 사진으로, W컨셉은 서로 다른 소재 2개로 정상 확인

## 12차: 바바더닷컴 메타소재 수동 보정 (2026-07-29)
- [x] 사용자가 지적: 바바더닷컴 메타소재 2개가 스튜디오 배경 모델 착용컷(사실상 제품샷에 가까움)으로 자동 선택돼 있었음 — `looks_like_content_photo()` 휴리스틱이 모델 유무만으로는 스튜디오 컷과 라이프스타일 컷을 못 가림을 확인
- [x] 후보 12개를 모아 직접 육안으로 확인 → IZZAT BABA(리조트룩 그래픽 콘텐츠)·JIGOTT(요트 라이프스타일) 2개로 교체
- [x] 대시보드에서 실제 반영 확인
- [ ] (향후 개선 필요) 자동 새로고침 시에도 이 수준으로 걸러지도록 휴리스틱 보강 — 현재는 사람이 직접 골라야 확실함

## 13차: 주요 키워드(해시태그) + 코멘트 섹션 추가 (2026-07-29)
- [x] 사용자가 기존에 수기로 관리하던 스프레드시트(주요 키워드 해시태그 + Comment 분석)와 같은 형태를 대시보드에서도 보고 싶다고 요청
- [x] 키워드는 브랜드검색 박스 썸네일 캡션에서 자동 추출 (`extract_keywords_from_box()`) — `data/competitor_monitor/keywords.csv`, 대시보드에 해시태그 pill로 표시
- [x] 코멘트(소재 해석/전략 메모)는 자동 생성하지 않고 마케터가 직접 작성하는 입력란으로 구현 — `data/competitor_monitor/comments.csv`, "코멘트 작성/수정" expander
- [x] 새로고침으로 실제 4개 브랜드 키워드 추출 확인, 코멘트 저장/표시 테스트 완료

## 14차: 디자인 가독성 개선 + 주간 아카이브 (2026-07-28)
- [x] 브랜드명(더한섬닷컴/신세계V/W컨셉/바바더닷컴)이 잘 안 보인다는 피드백 → 블루 그라디언트 헤더 바 + 흰색 21px 굵은 글씨(`brand-header`/`brand-title`)로 브랜드 블록 상단에 강조 배치
- [x] 코멘트가 잘 안 보인다는 피드백 → 앰버(#F59E0B) 톤 콜아웃 박스 + "코멘트" 라벨 필(`comment-box`/`comment-label`)로 재구성 (기존 1-액센트-컬러 원칙에서 강조 목적으로 의도적으로 벗어남)
- [x] 사용자의 기존 Google Sheets 탭 이름("26년 7월 4주차 커머스 광고" 등) 규칙을 그대로 재현하는 `week_of_month()`/`week_label()` 작성, 여러 날짜로 검증(7/6→1주차 ~ 8/3→1주차 등 확인)
- [x] `scraper/weekly_archive.py` 신규 작성 — 매주 월요일 실행 시 `competitor_monitor_scraper.py`의 캡처 로직 재사용 + `data/competitor_monitor/archive_weeks.csv`에 그 주 라벨 upsert. 실제 프로덕션 1회 실행으로 "26년 7월 4주차" 기록 확인
- [x] 대시보드에 "주차 아카이브에서 보기" 드롭다운 추가 (`load_archive_weeks()`) — 과거 주차 선택 시 해당 날짜로 자동 조회
- [x] Windows 작업 스케줄러에 `BabaderWeeklyArchive` 등록 — 매주 월요일 12:00(정오) 자동 실행, `schtasks /query`로 다음 실행 시각이 "2026-08-03 오후 12:00:00"임을 확인
- [x] CLAUDE.md에 `archive_weeks.csv` 데이터 계약 + 주차 계산 로직이 `app.py`/`weekly_archive.py` 양쪽에 복제되어 있다는 주의사항 기록

## 14-2차: 사이드바 아카이브 네비게이션 + 퍼플 리디자인 (2026-07-28)
- [x] "주차 아카이브에서 보기"를 메인 화면 상단 셀렉트박스에서 사이드바 "경쟁사 모니터링" 하위 화살표(▸/▾) 토글로 이동 — 클릭 시 그 자리에서 주차 목록이 펼쳐지고 다시 누르면 접힘(`st.expander` 대신 세션 상태 기반 커스텀 토글 버튼으로 구현, 참고 이미지의 사이드바 서브메뉴 패턴 재현)
- [x] 참고 디자인(Spik 어드민 대시보드) 스타일에 맞춰 전체 톤을 블루 → 퍼플/라벤더로 전면 교체: 흰색 라운드 사이드바(앱 배경과 분리된 카드처럼), 통계 카드는 라벤더/블루/피치 파스텔 배경 순환, 브랜드 헤더는 퍼플 그라디언트
- [x] Playwright로 실제 클릭까지 검증 (사이드바 토글 펼침/접힘, 디자인 스크린샷)

## 15차: 대시보드 상품화 폴리싱 + 스케줄 개편 (2026-07-28)
- [x] 앱 실시간 검색어 새로고침 카드에 캡처 시각까지 노출 — `input_at`은 원래 초 단위까지 저장돼 있었으나 카드엔 날짜만 표시하던 것을 `HH:MM`까지 잘라 `"자동 캡처 · 2026-07-28 12:26"` 형태로 표시 (`latest_channel_df()` 반환값에 `input_time` 추가)
- [x] "판매 가능한 퀄리티" + "영업회의 화면 공유" 요구에 맞춘 폴리싱 패스:
  - 브랜드 요약 카드 신규 추가 — 경쟁사 모니터링 페이지 최상단에 4개 브랜드를 압축 그리드로 보여주고, 클릭하면 해당 브랜드 상세 블록으로 앵커 점프(`id="brand-{n}"` + `<a href="#...">`, 회의 시작 시 스크롤 없이 전체를 훑고 필요한 곳만 점프)
  - 통계 카드에 이모지 아이콘 칩 추가 (`stat_row()` 시그니처를 `(label, value, icon)`로 확장)
  - 공통 페이지 헤더 컴포넌트(`page_header()`) 신설 — 제목/설명 옆에 "최신 기준일"·"주차" 배지를 나란히 배치, 두 페이지(실시간 인기 검색어/경쟁사 모니터링)에 동일 적용
  - `st.success/error/info` 등 알림 배너를 스트림릿 기본 회색/빨강 대신 퍼플 팔레트 톤(좌측 컬러 바 + 라운드)으로 커스텀
  - 타이포 소폭 상향(통계 숫자 26→28px, 카드 제목 16→17px, 브랜드명 21→22px) — 회의실 화면 공유 시 가독성 고려
  - 범위 밖으로 명시: 주차 간 수치 비교/트렌드 배지는 아카이브가 2주치뿐이라 보류(정확도 원칙), 외부 아이콘 폰트 미도입(이모지로 대체)
- [x] 스케줄 개편 (사용자 확인: 슬랙은 Incoming Webhook, 월요일 업데이트 범위는 경쟁사 모니터링만, 기존 목요일 작업은 삭제):
  - `scraper/weekly_archive.py`: 주차 라벨을 실행일이 아니라 **그 주의 월요일**로 앵커링하는 `week_label_for()` 추가(금요일 실행으로 바뀌어도 라벨이 흔들리지 않도록) — 여러 날짜로 검증(7/31→7월4주차, 8/7→8월1주차 등)
  - `scraper/monday_update.py` 신규 작성 — 경쟁사 모니터링 재수집 후 `.streamlit/secrets.toml`의 `slack_webhook_url`로 성공/실패 요약을 슬랙에 알림(웹훅 없으면 SKIP 로그 명확히 남김, 새 의존성 없이 표준 라이브러리 `tomllib`/`urllib.request`만 사용)
  - Windows 작업 스케줄러: `BabaderCompetitorMonitor`(목요일 17시) 삭제 → `BabaderWeeklyArchive`를 금요일 10:00으로 재등록 → `BabaderMondayUpdate`(월요일 09:30) 신규 등록, `schtasks /query`로 각각 다음 실행 시각 확인(2026-07-31 10:00 / 2026-08-03 09:30)
- [x] 대시보드 캡션 문구를 새 스케줄("매주 금요일 오전 10시 자동 아카이브 · 매주 월요일 오전 9시 30분 자동 업데이트+슬랙 알림")로 갱신
- [x] Playwright로 전체 재검증 (앱 검색어 시각 표시, 요약 카드 앵커 클릭, 아이콘 칩, 알림 스타일) — 스크린샷 확인 완료

## 16차: 기존 스프레드시트 레이아웃 재현 + 디테일 수정 (2026-07-28)
- [x] "이번 주 요약" 카드의 브랜드명/코멘트에 밑줄이 남아있던 문제 수정 — Streamlit 마크다운 컨테이너의 기본 링크 밑줄 스타일이 우리 CSS보다 우선 적용되고 있었음, `.summary-card`와 그 자식 요소 전체에 `text-decoration: none !important` 적용
- [x] 사용자가 기존에 쓰던 경쟁사 동향 Google Sheets(주차별 탭, "1-2. 브랜드검색" 식 번호 매김 + 이미지 아래 회색 캡션 바 레이아웃)와 최대한 비슷하게 재구성:
  - 브랜드 헤더를 "1. 더한섬닷컴" 식으로 번호 매김
  - "브랜드검색"/"메타 광고 소재" 서브섹션 라벨을 "1-1. 브랜드검색"/"1-2. 메타 광고 소재"로 번호 매김 + 회색 바 스타일(`group-label` CSS 변경)
  - 이미지 카드의 제목/등록일을 이미지 위가 아니라 **이미지 아래 회색 캡션 바**로 이동(`render_media_card()` 재구성, `.media-caption`) — 시트의 "WEB_이벤트 페이지" 캡션 바 느낌 재현
  - 페이지 상단에 "경쟁사 리스트 · 더한섬닷컴 · 신세계V · W컨셉 · 바바더닷컴" 한 줄 추가 (시트 상단의 "경쟁사 리스트" 섹션과 동일한 정보)
- [x] "새로고침" 버튼 등 메인 화면 액션 버튼을 기본 흰색 테두리 박스에서 퍼플 그라디언트 필 버튼으로 교체(`stButton`/`stFormSubmitButton` 전용 CSS) — 사용자가 "너무 안 예쁘다"고 지적한 부분
- [x] 새로고침 성공 시 뜨던 "수집 로그 보기" expander 제거 (실패 시에만 로그 노출, 성공 시엔 간결한 배너만)
- [x] Playwright로 전체 재검증 (밑줄 제거, 번호 매김, 캡션 바, 버튼 스타일, 성공 배너) — 스크린샷 확인 완료

## 향후 (범위 밖, 별도 논의)
- [ ] 무신사/29CM 등 대체 자동 수집 채널 조사
- [ ] 자사 성과 대시보드와 연동 여부 논의
